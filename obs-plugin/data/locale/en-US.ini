IOSCameraSource="LensLink Camera"
ScreenSource="LensLink Screen"
Mode="Connection"
Mode.Dial="Wi-Fi — enter the phone's IP"
Mode.USB="USB cable"
Host="Phone IP (shown in the LensLink app)"
UsbDevice="USB device"
UsbDevice.Auto="Auto (first available)"
LowLatency="Low latency mode (render newest frame immediately)"
HwDecode="Hardware decoding (GPU) — falls back to software automatically"
AudioSource="Lip-sync audio source"
AudioSource.None="(none)"
AudioSync="Automatically delay that audio source to match the camera latency"
AutoCalibrate="Auto-calibrate from the phone mic (enable 'Auto lip-sync reference' in the app; ignores the manual latency below)"
AutoVideoDelay="Auto video delay for slow mics (delays the video to match a mic that's slower than the stream)"
AudioLatency="Audio device's own latency (subtracted from the delay; manual mode only)"
WebControl="Browser control panel at http://localhost:9980 (zoom/focus/exposure)"
Diagnostics="Verbose diagnostics in the OBS log"
Diagnostics.Desc="Writes a per-connection pipeline heartbeat (packets, decoded frames, bitrate) and the phone's own counters to the OBS log. Turn on when a stream won't start; leave off for normal use."
Status="Status"
Status.Connected="Connected:"
Status.Disconnected="Disconnected — reconnecting"
Status.WaitingUSB="No iPhone detected on USB — check the cable and tap Trust (Windows also needs iTunes)"
Status.USBFound="iPhone found — start the camera in the LensLink app, or start a screen broadcast"
Status.Dialing="Trying to reach the phone at"
Status.NoHost="Enter the phone's IP address (shown in the LensLink app)"
Status.DeviceBusy="This device is already used by another LensLink source — a phone can feed only one source at a time"
Status.DecoderFailed="Connected, but this OBS build can't decode the stream — try H.264 in the app"
Status.CameraOnScreen="Phone is sending its camera — this is a LensLink Screen source. Start a screen broadcast on the phone, or use a LensLink Camera source."
Status.ScreenOnCamera="Phone is sending a screen broadcast — this is a LensLink Camera source. Use a LensLink Screen source for it."
Status.WaitingPinned="Waiting for the selected USB device to be connected"
HelpText="Open the LensLink app on your iPhone/iPad and tap Start. Then either enter the phone's IP address (shown in the app) above with Connection set to Wi-Fi, or plug the phone in with a USB cable and set Connection to USB (needs iTunes on Windows)."
HelpTextScreen="In the LensLink app on your iPhone/iPad, tap 'Mirror screen to OBS', pick LensLink Screen, and Start Broadcast. Connect over Wi-Fi (enter the phone's IP above) or USB cable (needs iTunes on Windows). The phone's system audio comes along; DRM apps (Apple Music, Netflix) mute themselves during broadcasts."
