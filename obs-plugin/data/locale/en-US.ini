IOSCameraSource="iOS Camera"
Mode="Connection"
Mode.Dial="Wi-Fi — enter the phone's IP"
Mode.USB="USB cable"
Host="Phone IP (shown in the OBSCam app)"
LowLatency="Low latency mode (render newest frame immediately)"
HwDecode="Hardware decoding (GPU) — falls back to software automatically"
AudioSource="Lip-sync audio source"
AudioSource.None="(none)"
AudioSync="Automatically delay that audio source to match the camera latency"
AutoCalibrate="Auto-calibrate from the phone mic (enable 'Auto lip-sync reference' in the app; ignores the manual latency below)"
AutoVideoDelay="Auto video delay for slow mics (delays the video to match a mic that's slower than the stream)"
AudioLatency="Audio device's own latency (subtracted from the delay; manual mode only)"
WebControl="Browser control panel at http://localhost:9980 (zoom/focus/exposure)"
Status="Status"
Status.Connected="Connected:"
Status.Disconnected="Disconnected — reconnecting"
Status.WaitingUSB="Waiting for iPhone on USB (open the OBSCam app and tap Start)"
Status.Dialing="Trying to reach the phone at"
Status.NoHost="Enter the phone's IP address (shown in the OBSCam app)"
HelpText="Open the OBSCam app on your iPhone/iPad and tap Start. Then either enter the phone's IP address (shown in the app) above with Connection set to Wi-Fi, or plug the phone in with a USB cable and set Connection to USB (needs iTunes on Windows)."
